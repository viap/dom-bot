export enum FORM_INPUT_TYPES {
  FLOAT = "float",
  NUMBER = "number",
  STRING = "string",
  BOOLEAN = "boolean",
  SELECT = "select",
  DATE = "date",
  // PRICE = "price",
  // COMPLEX = "complex",
}
